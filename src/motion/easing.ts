// ONE shared easing curve for the entire site (AGENTS.md, LOCKED).
// Mirrors the `brand` timing function in tailwind.config.js.
export const BRAND_EASE = [0.22, 1, 0.36, 1] as const;
export const DURATION = 0.7;
